import React from 'react';

// Este arquivo estava com JSX corrompido e não é mais utilizado pelo app (substituído por AutomacaoPDF2).
// Mantemos um componente mínimo apenas para evitar que o TypeScript acuse erros de compilação.

function AutomacaoPDF() {
  return (
    <div style={{ padding: 16 }}>
      <p style={{ fontSize: 14 }}>
        Este componente foi substituído por <strong>AutomacaoPDF2</strong>. Utilize o menu principal para acessar a versão atual.
      </p>
    </div>
  );
}

export default AutomacaoPDF;
export { AutomacaoPDF };
 
